RED='\033[0;31m'
GREEN='\033[0;32m'
NC='\033[0m' # No Color (Reset)

git checkout main
git pull origin main
git checkout -
git rebase main

if [ $? -ne 0 ]; then
  echo "${RED}💥 Rebase failed. Please resolve conflicts and try again."
  exit 1
fi

echo "${GREEN}===== ✅ Ready to push =====${NC}\n"
